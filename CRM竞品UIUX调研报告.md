# CRM 竞品 UI/UX 深度调研报告

> 调研时间：2026-09-13 | 调研对象：iBizCRM（实机）+ GitHub 高星开源 CRM Top3

---

## 一、iBizCRM 实机分析

**访问方式**：浏览器打开 `https://crm.ibizlab.cn/ibizcrm-crmweb/`，使用演示账号 `demo_admin / 123456` 登录。

### 1.1 整体布局

| 区域 | 设计 | 评价 |
|------|------|------|
| 左侧导航 | 纯图标 Rail（蓝色主题），hover 展开文字 | 节省空间，但新用户学习成本高 |
| 二级导航 | 图标点击后左侧滑出筛选面板（占约 40% 宽度） | 筛选能力强，但挤占主内容区 |
| 顶部 | 面包屑 + 视图切换器（表格/卡片/看板）+ 全局操作 | 标准模式 |
| 内容区 | 多 Tab 管理，可同时打开多条记录 | 适合高频操作，但 Tab 过多易混乱 |

### 1.2 列表页

- **多视图切换**：表格视图 / 卡片视图 / 看板视图（看板视图加载较慢，偶发卡顿）
- **搜索框**：⚠️ 搜索的是「筛选项名称」而非数据内容——输入客户名无反应，这是错误设计
- **筛选**：左侧面板多条件组合筛选，条件间 AND/OR 可配
- **批量操作**：表头复选框全选，底部出现批量操作栏
- **行内操作**：hover 行尾出现编辑/删除按钮

### 1.3 详情页

- Tab 结构：基本信息 / 关联客户 / 联系人 / 商机 / 活动记录 / 附件
- 字段展示：标签-值两列布局，密集但信息完整
- 关联列表：每个 Tab 内嵌套子表格，支持新增/关联
- 时间线：活动记录以时间线形式展示

### 1.4 商机页

- ⚠️ **无看板/Pipeline 视图**：仅表格列表 + 阶段筛选
- 阶段字段：下拉选择，无拖拽推进
- 金额展示：列表列显示，无汇总 KPI

**结论**：iBizCRM 功能全面但交互偏重，搜索设计有缺陷，商机无看板——我们的 Pipeline 看板设计已领先。

---

## 二、GitHub 高星开源 CRM Top3

### 2.1 Twenty（56K+ stars）— 最值得对标

**官网**：twenty.com | **技术栈**：React + TypeScript + NestJS

| 设计模式 | 具体实现 | 可借鉴度 |
|----------|----------|----------|
| **Ctrl+K 命令面板** | 全局搜索 + 快速创建 + 跳转 + 执行动作，一个弹窗完成 | ⭐⭐⭐⭐⭐ |
| **看板拖拽** | 商机卡片可跨列拖拽，拖拽时显示目标列高亮 + 放置预览 | ⭐⭐⭐⭐⭐ |
| **右侧滑出面板** | 点击记录不跳页，右侧滑出详情面板，保持列表上下文 | ⭐⭐⭐⭐⭐ |
| **侧边栏可定制** | 支持拖拽排序、分组、隐藏、添加收藏视图 | ⭐⭐⭐⭐ |
| **键盘快捷键** | `G then P` 跳转客户，`G then O` 跳转商机，`G then S` 设置 | ⭐⭐⭐⭐ |
| **面包屑导航** | 顶部显示当前路径，可点击回退 | ⭐⭐⭐ |

### 2.2 Chatwoot（29K+ stars）

**官网**：chatwoot.com | **定位**：客户对话管理平台

| 设计模式 | 具体实现 |
|----------|----------|
| **四段式布局** | 最左图标 Rail → 会话列表（中左）→ 对话详情（中）→ 联系人面板（右） |
| **虚拟滚动** | 会话列表使用虚拟滚动，千条数据不卡顿 |
| **筛选 Tab** | 列表顶部 Tab 切换：未分配 / 我的 / 团队 / 全部 |
| **未读标识** | 未读会话加粗 + 左侧蓝色竖条 |

### 2.3 ERPNext（33K+ stars）

**官网**：erpnext.com | **定位**：全功能 ERP（含 CRM 模块）

| 设计模式 | 具体实现 |
|----------|----------|
| **Workspaces 分组** | 左侧按业务域分组（会计/销售/采购/库存/CRM），每个 Workspace 是 landing page |
| **全局搜索** | 顶部搜索框跨 DocType 搜索，输入即出结果，支持快捷创建 |
| **列表水平滚动** | 列数不限，水平滚动查看，列宽可拖拽调整 |
| **三种视图** | 列表 / 表单 / 报表，工具栏切换 |
| **置顶常用** | 个人 Workspace 置顶，快速访问 |

---

## 三、可落地改进清单

### P0 — 立即修复（本次已完成 ✅）

| # | 改进项 | 状态 | 说明 |
|---|--------|------|------|
| 1 | 左侧导航底部空白 → 添加「系统」分组沉底（设置+帮助） | ✅ 已完成 | 参考 ERPNext Workspaces 分组，填充底部 60% 空白 |
| 2 | 全局搜索单字无结果 → 启用 MiniSearch prefix + fuzzy 匹配 | ✅ 已完成 | 参考 Twenty/ERPNext 全局搜索，输入「盛」即返回客户/商机/任务/笔记 |
| 3 | 商机页标题行技术术语 → 改为 3 张 KPI 卡片 | ✅ 已完成 | 删除「Deal 唯一漏斗」等英文术语，改为「在途商机 N 个 / 加权金额 ¥X / 比稿胜率 N%」 |
| 4 | 底部状态栏「本地优先·数据不上传」→ 删除 | ✅ 已完成 | 老板明确要求删除 |
| 5 | 底部用户菜单布局松散 → 头像左对齐，铃铛+主题右对齐 | ✅ 已完成 | 参考成熟应用底部用户区紧凑布局 |
| 6 | 浅色主题渲染异常 → CDP 验证双主题正常 | ✅ 已完成 | 浅色主题背景 rgb(245,246,248)，文字 rgb(23,24,28)，内容区 727px 正常渲染 |

### P1 — 下一轮迭代建议

| # | 改进项 | 为什么 | 怎么改 |
|---|--------|--------|--------|
| 7 | Pipeline 看板支持拖拽跨列 | 当前静态卡片墙，用户无法直观推进商机阶段 | 参考 Twenty：HTML5 drag & drop，拖拽时目标列高亮，放下后调用 db.put 更新 stage |
| 8 | Pipeline 列头显示汇总金额 | 当前仅显示数量，用户无法感知每阶段金额体量 | 列头 `阶段名 · ¥汇总金额 · N个`，金额=该列所有商机 value 之和 |
| 9 | 商机详情改为右侧滑出面板 | 当前点击卡片打开全屏 drawer，丢失列表上下文 | 参考 Twenty：右侧 480px 滑出，遮罩点击关闭，列表保持可见 |
| 10 | 空状态引导优化 | 当前空状态仅有图标+文字，缺少行动指引 | 参考成熟产品：空状态 + 「新增第一个商机」主按钮 + 3 步使用引导 |
| 11 | 列表页支持列宽调整 | 当前表格列宽固定，长文本截断 | 参考 ERPNext：列头边缘拖拽调整，宽度持久化到 localStorage |

### P2 — 长期规划

| # | 改进项 | 为什么 |
|---|--------|--------|
| 12 | Ctrl+K 命令面板 | 参考 Twenty，全局搜索 + 快速创建 + 跳转合一，提升高频操作效率 |
| 13 | 侧边栏可定制 | 支持拖拽排序、分组、隐藏，适配不同角色使用习惯 |
| 14 | 键盘快捷键体系 | `G+客户`跳转客户管理，`G+商机`跳转商机，`N`新建，参考 Twenty/Vim 模式 |

---

## 四、本次修复验证结果

| 验证项 | 结果 |
|--------|------|
| TypeScript typecheck | ✅ 0 错误 |
| 单元测试 (vitest) | ✅ 36/36 通过 |
| CDP 深色主题渲染 | ✅ 正常 |
| CDP 浅色主题渲染 | ✅ 正常（背景/文字/布局均正确） |
| 搜索「盛」返回结果 | ✅ 客户1条 + 商机1条 + 任务1条 + 笔记1条 |
| 左侧导航系统分组沉底 | ✅ 常用(1) + 业务(7) + 系统(2)，无底部空白 |
| 底部状态栏已删除 | ✅ 无「本地优先·数据不上传」 |
| 商机 KPI 卡片 | ✅ 3 列 3 卡，术语已清除 |

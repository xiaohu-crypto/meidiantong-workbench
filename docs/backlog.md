# 媒电通工作台 · 问题清单（GitHub Issues 待建）

> 本清单为「问题跟踪」的权威来源，用于替代 `delivery/issue-tracker`（localStorage 录入器）。
> 创建方式（宿主执行）：`gh issue create --repo xiaohu-crypto/meidiantong-workbench --title "..." --body "..." --label "..."`
> 或逐条复制以下条目到 GitHub Issues。标签规范见每条。

---

## 🔴 P1（本轮必修）

### [P1] CRM 客户详情 5 Tab → 左25%/中50%/右25% 一体化工工作台
- **模块**：客户管理 > 客户列表 > 点击客户名称弹出的详情
- **类型**：UI优化
- **标签**：`P1` `UI优化` `CRM`
- **现状**：当前「概览 / 跟进 / 决策链 / 媒介策略 / AI 建议」5 个独立顶层 Tab，信息割裂、留白过多、AI 面板无法配合基础资料对比。
- **预期**：重构为三栏一体化工工作台；决策链 / 媒介策略走右侧连续滑出多层抽屉；媒介策略「预算配比」非 100% 时拦截提交并标红。
- **验收**：旧 Tab 完全移除；两层抽屉动画 ≤250ms；预算非 100% 标红拦截；保存后主页面与 AI 面板热加载。
- **规格**：见 `docs/crm-drawer-3col-spec.md`

---

## 🟠 P2（有空就修 / 技术债）

### [P2] 状态管理：props drilling → Zustand/Context
- **类型**：技术债
- **标签**：`P2` `重构` `技术债`
- **现状**：`src/App.tsx` 用 `loadAll()` 取全量 data 经 props 下传所有页面。
- **预期**：引入 Zustand 或 Context，减少透传，提升可维护性。

### [P2] E2E 关键路径补全
- **类型**：测试
- **标签**：`P2` `测试` `E2E`
- **现状**：`tests/e2e/app.e2e.ts` 仅 4 用例（Playwright + Electron）。
- **预期**：补 导入（多格式）/ CRM 详情 / 工作流触发 关键路径 E2E。

### [P2] 崩溃上报
- **类型**：稳定性
- **标签**：`P2` `稳定性`
- **现状**：ErrorBoundary 仅本地显示友好提示。
- **预期**：接本地 dump 或 Sentry，崩溃可回溯。

### [P2] 应用图标缺失
- **类型**：一致性问题
- **标签**：`P2` `打包` `UI`
- **现状**：rcedit 设置图标报 "Fatal error" 已绕过，产品无自定义图标。
- **预期**：补 .ico 并修复打包配置。

---

## 🟡 P3（后续）

### [P3] 飞书表格粘贴 / 企微通讯录导入
- **类型**：功能需求
- **标签**：`P3` `导入`
- **现状**：外部数据导入已支持多格式文件，但未接飞书 / 企微。

### [P3] deep link（媒电通:// 协议）
- **类型**：功能需求
- **标签**：`P3` `增强`
- **现状**：不支持协议唤起。

---

## ✅ 已闭环（复盘对齐批次）

- [x] **文档漂移**：README.md / HANDOFF.md 已从 v0.1.0 旧架构对齐至 v0.2.0 NocoBase 内核（2026-09-23）
- [x] **仓库 URL 错误**：旧文档误写 `mediantong-releases`，已修正为 `meidiantong-workbench`（2026-09-23）
- [x] **版本号**：package.json 0.1.0 → 0.2.0（2026-09-23）
- [x] **v2.1/v2.2 悬置**：决策归档为历史参考，不再执行（见 docs/ROADMAP.md）
- [x] **问题跟踪脱节**：localStorage 录入器 → GitHub Issues（本清单）

# 媒电通工作台（MediaDesk）深度测评与改进方案 · v1

> 审查对象：`D:\HaLeMa\Documents\Obsidianku\原始资料\autoclaw\app`
> 仓库：`https://github.com/xiaohu-crypto/mediantong-releases`（HANDOFF 标注，最新 commit `60b2fe0`）
> 版本：v0.1.0 · 技术栈：Electron 33 + React 18 + TS strict + Vite 5 + IndexedDB(AES-256-GCM) + OpenRouter
> 审查时间：2026-09-11 · 方法：全量源码静态走查 + 逐窗口功能推演 + 市场对标
> 状态：**方案评审稿，待老板确认范围与优先级后再执行**

---

## 〇、审查范围与前置说明

- 你提供的 GitHub 地址 `https://github.com/仙姑网址` 为无效占位符，已按 HANDOFF 文档修正为真实仓库 `xiaohu-crypto/mediantong-releases`（已用本地 `git remote -v` 对账一致）。
- 本次已**只读**通读 33 个源文件（11 页面 + 6 组件 + 11 核心模块 + 类型/数据/样式），并对 3 个高危疑似缺陷做了源码级行号核实（见第三章）。
- 本稿**不含任何代码改动**，仅交付测评结论与改进方案，等你确认后进入执行。

---

## 一、评估框架

### 1.1 六维评估模型

| 维度 | 代号 | 评估要点 |
|---|---|---|
| 功能完整性 | F | 用户能做什么；是否闭环；有无假交互/半成品 |
| 交互体验 | UX | 导航、快捷键、抽屉/弹窗、空态、加载、撤销、反馈 |
| 视觉识别 | VI | 设计 token 一致性、品牌资产（图标/配色/字体）、响应式、明暗主题 |
| 代码质量 | CQ | 类型安全、异常/空值处理、重复逻辑、可测试性、潜在 bug |
| 安全与数据 | S | 加密、密钥管理、备份、软删/回收站、数据毁灭风险 |
| 对标差距 | G | 相对市场主流同类产品的功能缺口 |

### 1.2 评分标尺

- 🔴 严重（阻断/数据风险/假交互伪装）——必须修
- 🟠 高（功能正确性 bug / 半闭环）——应修
- 🟡 中（一致性/可访问性/健壮性）——建议修
- 🟢 良（达标或优于预期）

### 1.3 覆盖矩阵（11 个窗口 + 3 层底座）

```
窗口层：Today / CRM / Work / Dev / Media / Kb / Data / Growth / Settings / Notifications / Help
全局层：Sidebar / Topbar / TopSearch / QuickCapture / 快捷键 / 通知铃铛
底座层：db(vault) / AI(client·quota·script·router·desensitize) / search / notify / electron(main·preload)
```

---

## 二、10 轮系统性测评（逐窗口·含真实发现）

> 每轮 = 一个窗口/层 + 全维度打分 + 关键发现（行号已核实）。

### R1 · 启动与外壳层（Electron main / preload / Onboarding / ErrorBoundary / 自动更新 / 自动备份）
- **F**：引导三选一（示例/导入/空白）✅；自动更新静默检查 ✅；自动备份定时器**已在 `App.tsx` 实现**（60s tick + 轮转保留）✅，HANDOFF 称"已有"属实。
- **S**：备份为**明文 JSON**（未加密），与"静态加密"护城河不一致 ⚠️ G。
- **VI**：`ErrorBoundary` 兜底页颜色硬编码 `#2563eb/#888/#f5f5f5`，**不随明暗主题切换** 🟡；应用**无品牌图标**（rcedit Fatal 绕过），exe/任务栏无 logo ⚠️ VI 缺口。
- **CQ**：`Onboarding` 用 `window.alert` 而非全局 toast，风格不统一 🟡。
- **S**：安装包**未签名**，用户首次运行触发 Windows SmartScreen 拦截，影响分发信任 🟠。

### R2 · 全局导航与快捷交互（Sidebar / Topbar / TopSearch / QuickCapture / 快捷键 / 铃铛）
- **UX**：`Ctrl+1~9` 切换 9 视图 ✅；`Ctrl+K` 快速采集可改键 ✅；搜索 ↑↓/Enter/Esc 键盘完备 ✅。
- **F**：`TopSearch` 索引在 `App` 数据变更后 `rebuildIndex` ✅（已核实，无陈旧风险）；铃铛未读数 = 逾期回款 + 14 天无接触客户 ✅。
- **UX 不一致**：`Notifications` 是一个完整视图，却**不在侧边栏 NAV 中**，仅能经铃铛进入；侧栏 10 项、快捷键映射与侧栏顺序不一致（Ctrl+2=Work、Ctrl+3=CRM）🟡。
- **CQ**：`QuickCapture` 行 39 `const seen = new Set(...); void seen;` 为**死代码** 🟡；OCR/剪藏 tab 诚实标记"二期"禁用 ✅。
- **UX**：搜索结果未用 `role=listbox/option`，非标准 combobox（基本可用）🟡。

### R3 · 今日驾驶舱 Today
- **F**：NBA 规则引擎排序、里程碑倒计时、OKR KR、日程、alert-strip（健康度/超期/待回款）✅。
- **F 半闭环**：「记录为已跟进」仅写入本地 state，**刷新即丢、不入库** 🟠；「稍后提醒」写 `settings.snoozed` 且 `App` 60s 定时已消费 ✅（已核实，**非死功能**，Explore 初判有误，已纠正）。
- **CQ**：`void props.reload;` 入参从未使用（死代码）🟡；`genScript` 云/本地回退闭合 ✅。
- **待核**：Explore 报告行 93–114 有游离 `'` 字符，但 `tsc` 通过且 build 产出，疑似误读，建议 `tsc --noEmit` 复核（不阻塞）。

### R4 · CRM 客户管理（列表 / 360 抽屉 6 Tab / 批量 / 导入 / 媒介策略）
- **F**：漏斗头、密度/列显隐/行业筛选/排序/视图持久化、行多选批量删（走回收站）、360 抽屉 6 Tab、客户包导出、媒介策略持久化、AI 建议 ✅。
- 🔴 **布局 bug（已核实行 465）**：`tab === "AI建议"` 的渲染块位于 `</aside>`（行 423）**之外**，选中该 Tab 时抽屉体空白、AI 内容掉到页面底部。
- 🟠 **假交互（已核实）**：抽屉底栏「记录跟进」仅 `show("演示:接触点快速记录属 P1 管线")`，**无任何写库**（自承 P1 未实现）。
- 🟡 时间线任务 `ts` 两分支等价（行 64）；`#f59e0b` 硬编码应改 `--warning`；批量删用 `window.confirm`（与 toast 不一致）；抽屉无 Esc 关闭。

### R5 · 工作管理系统 Work（看板 / 表格 / 日历 / OKR / 拖拽）
- **F**：四列看板拖拽、WIP 上限仅告警、表格、自绘月历、OKR 只读、任务增删改 + 撤销 toast ✅。
- 🟡 高优先级底色硬编码 `#fef2f2` 应改 `--danger-bg`；视图切换内联样式偏多；多处 fire-and-forget `void (async()=>{...})()` 无 `catch`。
- **待核**：Explore 报告文件末尾有孤立 `}`，`tsc` 通过，疑似误读，建议复核（不阻塞）。

### R6 · 客户开发系统 Dev（Pipeline / 比稿 / SOP / MEDDIC·BANT / 自定义阶段）
- **F**：7 列 Pipeline、阶段自动带概率、加权金额、MEDDIC/BANT 打标、比稿登记与胜率、AI 复盘（✦）✅。
- 🔴 **功能缺口 bug**：自定义阶段仅出现在编辑下拉，**Pipeline 看板只渲染 `STAGES.slice(0,7)`**，被设为自定义阶段的商机**静默不显示**（丢失）。
- 🟡 SOP 4 条硬编码模板，注释称"自定义维护属二期"；`PROB` 对自定义阶段有兜底 ✅。

### R7 · 媒介策略中心 Media（排期 / 资源刊例 / 达人库 / 报价器 / PostBuy）
- **F**：最完整模块。甘特排期（成本/报价/毛利/返点双口径）、资源/点位/刊例 CRUD、达人库 CRUD、PostBuy 录入+CSV 回填+导出、自助报价器+HTML 导出 ✅。
- 🟡 重复逻辑：5 处「Blob→a.click→revoke」与 HTML 报表模板可抽 `downloadBlob()`/统一模板（重构项）；甘特 `pct()` 依赖 `new Date(d)`，无效日期得 `NaN` 无校验；排期窗口硬编码 2026-09~12。
- **一致性**：`Media` 用健壮 CSV 正则，`ImportCustomers` 用朴素 `split(",")`（引号内含逗号会错位）——**两套解析不一致** 🟡。

### R8 · 知识库 Kb / 数据分析 Data / 个人成长 Growth
- **Kb**：PARA、双链、版本历史、图谱 SVG、AI 问答抽屉 ✅；浏览器模式 `chatStream` 缺失时静默 return 无提示 🟡；编辑器**无自动保存**（离开丢草稿）🟠；空 `Field` 残留死 UI。
- 🔴 **Data 毛利趋势 bug（已核实行 46）**：`s + (c.amount - mediaCost / Math.max(contracts.length, 1))` 运算符优先级错误，应为 `(c.amount - mediaCost) / len` 语义；当前实为非预期的"金额−平摊月成本"。KPI 总额（行 32/72 `kpiSign - mediaCost`）正确，**仅月度趋势图数值错误**。
- **Growth**：目标/实际、AAR 周复盘、能力自评 ✅；70-20-10 课程·阅读追踪仅静态文案（P2 未做）🟡。

### R9 · 系统管理 Settings / 通知中心 Notifications / 帮助 Help
- **Settings**：8 Tab 齐全；主题/标题栏/专注/自启/快捷键/加密状态/回收站/日志/标签治理/自定义字段 ✅；**加密备份未实现（明文 JSON）** ⚠️ G。
- 🔴🔴 **Notifications 数据毁灭 bug（已核实行 66）**：按钮文案「标记全部已读(演示)」实际执行 `await db.clearAll()` —— **清空整个数据库**，无 confirm、清空后无 reload。伪装成 demo 的毁灭性操作，必须立即修复（改真实"标记已读"或删除按钮）。
- **Help**：纯静态说明，无 bug ✅。

### R10 · 数据/AI/安全底座 + 市场对标 + 能力缺口总评
- **db/vault**：schema v1→v2 迁移、AES-256-GCM 透明加密、safeStorage 密钥保险库、软删/回收站、`putMany` 批量事务、`undoLog` ✅ 稳健。
- **AI**：`client`（额度检查+用量持久化）、`quota`（80%/100% 阈值）、`script`（云/本地路由+回退）、`router`（敏感判定）、`desensitize`（脱敏+还原）✅ 闭环健壮；`desensitize` 模块级 `counter` 共享态可改为局部 🟡。
- **search/notify**：MiniSearch 2-gram 中文分词、Notification + 免打扰跨午夜 + 8h 补发 ✅。
- **工程化**：仅 db 层 23 单测；**无组件测试/E2E 实质覆盖**；Bundle 未拆 vendor；无崩溃上报（Sentry）；无 deep link 🟠（见 G 章）。
- **对标结论**：见第四章。

---

## 三、现存问题清单（按严重度，源码已核实）

| # | 严重度 | 位置（已核实） | 问题 | 修复方向 |
|---|---|---|---|---|
| P0-1 | 🔴🔴 | Notifications.tsx:66 | 「标记全部已读(演示)」调用 `db.clearAll()` 清空全库 | 改为真实"标记已读"或移除按钮 + confirm + reload |
| P0-2 | 🔴 | CRM.tsx:465 | AI建议 Tab 渲染块掉到 `</aside>` 外，抽屉体空白 | 移入 drawer-body |
| P0-3 | 🔴 | Dev.tsx:96 | 自定义阶段商机在 Pipeline 静默不显示 | Pipeline 渲染扩展至自定义阶段 |
| P1-1 | 🟠 | Data.tsx:46 | 毛利月度趋势运算符优先级错误 | 改为 `(c.amount - mediaCost)/len` 或按需口径 |
| P1-2 | 🟠 | CRM.tsx（底栏） | 「记录跟进」假交互无写库 | 实现接触点快速记录（P1 管线） |
| P1-3 | 🟠 | Kb.tsx | 编辑器无自动保存 | 加自动存草稿/离开确认 |
| P1-4 | 🟠 | 分发 | 安装包未签名 → SmartScreen 拦截 | 代码签名（或明确告知用户） |
| P2-1 | 🟡 | ErrorBoundary.tsx | 兜底页颜色不随主题 | 引用 token |
| P2-2 | 🟡 | Today/Work/Media 等 | 硬编码颜色 `#f59e0b/#fef2f2` | 改用 --warning/--danger-bg |
| P2-3 | 🟡 | QuickCapture:39 / Today:19 | 死代码 `seen`/`props.reload` | 清理 |
| P2-4 | 🟡 | CRM/Onboarding/ImportCustomers | `window.confirm/alert` 与 toast 不一致 | 统一交互组件 |
| P2-5 | 🟡 | Media/ImportCustomers | CSV 解析两套实现 | 统一到 `core/importer` |
| P2-6 | 🟡 | 抽屉/Modal | 普遍无 Esc 关闭 / focus trap | 抽通用 Drawer/Modal |
| P2-7 | 🟡 | 搜索/抽屉 | 非标准 ARIA / 无键盘可达图谱 | a11y 补全 |
| P3-1 | 🟢 | App/Settings | 自动备份明文 JSON | 加密备份（下一迭代） |
| P3-2 | 🟢 | 工程化 | 无 E2E/组件测试、无 Sentry、未拆 vendor | 见 G-改进 |

---

## 四、市场主流产品对标与能力缺口（G）

### 4.1 对标对象（媒体广告代理垂直 + 通用 CRM/协作）
- 国内垂直/CRM：**纷享销客、销售易、卫瓴、探迹、红圈、麦客、加推、微盛·企微管家**
- 协作/知识：**飞书、钉钉、企业微信、Notion、语雀**
- 低代码：**简道云、轻流、伙伴云**
- 海外：**HubSpot、Pipedrive、Attio、Folk、Airtable**
- 广告监测：**秒针/AdMaster/热云**（代理常用但非 CRM）

### 4.2 差异化护城河（保持，不盲目追平）
本地优先 + 媒体广告垂直 + AES 静态加密 + 单机零部署。**不与飞书比功能广度**是正确定位。

### 4.3 能力缺口矩阵

| 能力 | 主流产品 | 本项目 | 缺口等级 | 建议 |
|---|---|---|---|---|
| 多人协作/团队/权限 RBAC | 飞书/纷享销客 标配 | 无（单机定位） | 设计性缺口 | 保持单机；若扩团队再做 |
| 移动端 | 全民标配 | 无 | 中 | 暂不做（护城河=桌面）；留 PWA 余地 |
| **广告平台 API 对接**（巨量引擎/腾讯广告/快手磁力/小红书蒲公英） | 监测工具标配 | 仅手动 PostBuy | **高** | 补自动回填，降录入成本 |
| 审批流 | 飞书/纷享销客 | 无 | 中 | 合同/回款审批（垂直刚需） |
| 电子合同/发票/财务对账 | 销售易/卫瓴 | 无 | 中 | 对接 e签宝/发票 API |
| 深度 BI/可视化 | Tableau/帆软 | 基础 KPI+漏斗 | 中 | 增加趋势下钻/同期对比 |
| **自动化工作流**（类飞书多维表自动化） | 标配 | 无 | 高 | 触发器：回款逾期→通知→AI 拟跟进 |
| **AI 深度**（客户画像 embedding 检索/预测签约/自动周报） | HubSpot AI | 仅话术/复盘 | 高 | 接本地向量检索 + 周报生成 |
| 离线→同步→冲突解决 | 云原生 | 纯本地 | 设计性 | 保持；备选导出导入合并 |
| 数据导入渠道（飞书表格/企微通讯录） | 标配 | 仅 CSV/xlsx | 中 | 补粘贴导入 |
| 崩溃上报/遥测 | Sentry 标配 | 仅本地 ErrorBoundary | 中 | 接 Sentry（脱敏） |
| 加密备份 | 云自动 | 明文 JSON | 中 | AES 加密备份 |
| 组件/E2E 测试 | 标配 | 仅 db 23 单测 | 高（质量债） | 上 Playwright E2E + 组件测试 |
| 图标/品牌 VI | 标配 | 无图标 | 中 | 补 .ico/.icns + 品牌规范 |

### 4.4 优先级建议（补齐顺序）
1. **G-高·自动化工作流 + AI 深度**（差异化最大，契合"AI 辅助"定位）
2. **G-高·广告平台 API 自动回填**（直击代理录入痛点）
3. **G-中·审批流/电子合同/财务对账**（垂直刚需闭环）
4. **G-中·导入渠道/崩溃上报/加密备份/测试体系**（工程质量基线）
5. **VI·图标与品牌规范**（分发体感）

---

## 五、功能补齐建议（落地清单，分优先级）

- **P0（安全/正确性，必做）**：P0-1~P0-3 + P1-1~P1-3。
- **P1（垂直闭环）**：接触点快速记录；编辑器自动保存；告警/通知真实"已读"态；自定义阶段全链路显示。
- **P2（一致性/健壮性）**：统一 Drawer/Modal/Esc/focus-trap；统一下载与 CSV 解析；清理硬编码色与死代码；A11y。
- **P3（能力缺口，按 4.4 顺序）**：自动化工作流引擎 → 广告平台 API 回填 → 审批/合同/对账 → 导入渠道/遥测/加密备份/测试 → IV/品牌。

---

## 六、改进执行路线（分阶段，待确认）

| 阶段 | 范围 | 交付 | 门禁 |
|---|---|---|---|
| **Phase 0** | 安全熔断：P0-1 clearAll 修复 | 通知"已读"真实化 + 防误清 | 单测覆盖 + `tsc` 0 错 |
| **Phase 1** | 正确性：P0-2/P0-3/P1-1 | AI Tab 归位、自定义阶段显示、毛利趋势修正 | E2E 验证 |
| **Phase 2** | 半闭环补全：P1-2/P1-3 | 接触点记录、编辑器自动保存 | 手动验收 |
| **Phase 3** | 一致性/健壮性：P2 全项 | 统一组件、去硬编码、a11y | `tsc` + lint |
| **Phase 4** | 能力缺口（按 4.4） | 自动化/AI/API 回填/审批/测试/VI | 分期评审 |
| **Phase 5** | 工程化：测试/E2E/Sentry/vendor 拆分/签名 | CI 门禁 + 可分发 | 发版前 |

> 每阶段遵循"复盘确认 → 实物验证 → 补完空白 → 切入开发"，端到端 `tsc` + `vitest` + 构建验证后方可推进，遵循你既定的"先规划审批再实施"铁律。

---

## 七、待确认事项（执行前需要你拍板）

1. **范围**：本次先只做 Phase 0（安全熔断，最高优先级），还是一次性推进到 Phase 3（含一致性）？Phase 4 能力缺口建议单独立项评审。
2. **P0-1 修复姿态**：按钮改为"真实标记已读"（需新增通知已读字段/模型）还是直接移除该按钮？我倾向**真实标记已读**，但需要加数据模型字段。
3. **是否补组件测试/E2E**：是否允许引入 Playwright E2E 与组件测试作为 Phase 3/5 一部分（会新增 devDependencies 与 CI 步骤）。
4. **VI/签名**：是否启动品牌图标设计与代码签名（涉及费用/证书）？
5. **确认仓库归属**：当前本地 git 指向 `mediantong-releases`，是否即你所指的"GitHub 仓库"？

---

_本稿为评审版，未改动任何源码。确认范围与优先级后，我将按阶段执行并逐窗口回归验证。_

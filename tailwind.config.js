/** @type {import('tailwindcss').Config} */
// 媒电通工作台 v0.2.2 · Tailwind（仅启用 utilities，不启用 base，避免重置既有样式）
module.exports = {
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
